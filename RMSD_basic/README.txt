To perform analysis on a given system for all three replicates, you need to combine the individual data files for each replicate into one data file. In order to do this, use the cat function within the terminal. cat data_file1 data_file2 data_file3 > new_data_file_with_all_three_replicates

Next run the rmsd_plotting.py script
